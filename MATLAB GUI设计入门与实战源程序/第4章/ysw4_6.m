% text使用
clc,clear,close all % 清存
warning off         % 取消警告
feature jit off     % 加速代码执行
bar3(peaks(30))
axis tight
set(gcf,'color',[1,1,1])
text(10,10,9,'bar型图')
%% 字体颜色
% text使用
clc,clear,close all % 清存
warning off         % 取消警告
feature jit off     % 加速代码执行
bar3(peaks(30))
axis tight
set(gcf,'color',[1,1,1])
text(10,10,9,'bar型图','Color',[1 0 0])
%% 字体大小
% text使用
clc,clear,close all % 清存
warning off         % 取消警告
feature jit off     % 加速代码执行
bar3(peaks(30))
axis tight
set(gcf,'color',[1,1,1])
text(12,12,10,'bar型图','FontSize',16,'Color',[1 0 0])
%% 字体斜体
% text使用
clc,clear,close all % 清存
warning off         % 取消警告
feature jit off     % 加速代码执行
bar3(peaks(30))
axis tight
set(gcf,'color',[1,1,1])
text(12,12,10,'bar型图','FontSize',16,'FontAngle','italic','Color',[1 0 0])
%% 字体倾斜
% text使用
clc,clear,close all % 清存
warning off         % 取消警告
feature jit off     % 加速代码执行
bar3(peaks(30))
axis tight
set(gcf,'color',[1,1,1])
text(12,12,10,'bar型图','FontSize',16,'FontAngle','oblique','Color',[1 0 0])
%% 字体加粗
% text使用
clc,clear,close all % 清存
warning off         % 取消警告
feature jit off     % 加速代码执行
bar3(peaks(30))
axis tight
set(gcf,'color',[1,1,1])
text(12,12,10,'bar型图','FontWeight','bold','FontSize',16,'Color',[1 0 0])


